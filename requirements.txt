aws-cdk-lib>=2.170.0
constructs>=10.0.0
# Pinned below 3.0 deliberately: cdk-nag 3.0.1 (latest as of this build) has
# a real jsii-compatibility bug against this aws-cdk-lib version --
# AwsSolutionsChecks.visit is not callable as an Aspect, confirmed by
# actually running it, not assumed. 3.x also replaced NagSuppressions with
# CDK's native Validations.of().acknowledge(), which this project doesn't
# use (see the suppression calls in app.py/*_stack.py, if any -- they target
# the mature, well-documented 2.x NagSuppressions API instead).
cdk-nag>=2.28.0,<3.0.0
